Same day request

Email:
<%= @requestrecord.signup.email %>

Item and email of lenders, doesn't account for duplication, just spits out what's on borrow index page:
<% @requestlist.each do |index, item_hash| %>
	<%= index %>:
	<% item_hash.each do |item, email| %>
		<%= item %>, <%= email %> 
	<% end %>   
<% end %>

Additional details:
<%= @requestrecord.detail %>

Pickup date:
<%= @requestrecord.pickupdate %>

Return date:
<%= @requestrecord.returndate %>

Neighborhood
<%= @requestrecord.signup.streetone %>
<%= @requestrecord.signup.streettwo %>
<%= @requestrecord.signup.zipcode %>

Heard
<%= @requestrecord.signup.heard %>