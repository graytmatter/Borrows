New inventory

Email:
<%= @signup_parent.email %>

Item(s) saved this time:
<% @items_to_be_saved.each do |hashpair| %>
	<% hashpair.each do |label, itemlist_id| %>
  		<%= Itemlist.find_by_id(itemlist_id).name %>
  	<% end %>
<% end %>

