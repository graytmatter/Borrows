		<div class="row">
            <div class="col-xs-12">
                <% flash.each do |key, value| %>
                <div class="alert alert-<%= key %>", id="<%= key %>"><%= value %></div>
                <% end %>
            </div>
          </div>